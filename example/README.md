# flutter_lid_effect example

Run `flutter run -d macos` from this directory. Use the simulation switch and slider to preview closing and reopening without moving the physical lid. The demo keeps controls outside the transformed content.

The macOS example includes the HID entitlement required for a compatible built-in sensor, with App Sandbox enabled. See the parent package README for host integration.
