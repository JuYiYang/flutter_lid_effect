## 0.1.0

- Extract the application-local lid effect into a macOS Flutter plugin.
- Expose a shared typed angle/status stream and configurable effect widget.
- Keep child state, support reduced motion and reset on sleep/visibility changes.
- Include a standalone macOS example with simulated angle controls.
